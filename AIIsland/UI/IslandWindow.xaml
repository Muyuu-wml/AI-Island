<Window x:Class="AIIsland.UI.IslandWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="clr-namespace:AIIsland.UI"
 Title="AI Island" Width="420" MinWidth="420" MaxWidth="420" Height="66" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False" ShowActivated="False" FontFamily="Segoe UI Variable Text, Microsoft YaHei UI, Segoe UI" TextOptions.TextFormattingMode="Display" Foreground="#F3F4F8">
 <Window.Resources>
  <BooleanToVisibilityConverter x:Key="BoolVisibility"/>
  <Style x:Key="DrawerScrollStyle" TargetType="ScrollViewer">
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ScrollViewer">
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}"/>
     <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1" Width="10" Margin="3,2,0,2" Orientation="Vertical" Minimum="0" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Background="Transparent">
      <ScrollBar.Template><ControlTemplate TargetType="ScrollBar">
       <Grid Background="Transparent">
        <Track x:Name="PART_Track" Orientation="Vertical" IsDirectionReversed="True">
         <Track.DecreaseRepeatButton><RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False"><RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></RepeatButton.Template></RepeatButton></Track.DecreaseRepeatButton>
         <Track.Thumb><Thumb MinHeight="24" Cursor="Hand"><Thumb.Template><ControlTemplate TargetType="Thumb">
          <Grid Background="Transparent"><Border x:Name="Grip" Width="4" CornerRadius="2" Background="#4B5668" HorizontalAlignment="Center"/></Grid>
          <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Grip" Property="Background" Value="#8393AA"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="Grip" Property="Background" Value="#A6BCD8"/></Trigger></ControlTemplate.Triggers>
         </ControlTemplate></Thumb.Template></Thumb></Track.Thumb>
         <Track.IncreaseRepeatButton><RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False"><RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></RepeatButton.Template></RepeatButton></Track.IncreaseRepeatButton>
        </Track>
       </Grid>
      </ControlTemplate></ScrollBar.Template>
     </ScrollBar>
    </Grid>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style TargetType="Button">
   <Setter Property="Background" Value="#30343D"/><Setter Property="Foreground" Value="#F3F4F8"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="12,7"/><Setter Property="Margin" Value="0,0,6,0"/><Setter Property="Cursor" Value="Hand"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="Frame" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Frame" Property="Background" Value="#434B59"/></Trigger><Trigger Property="IsEnabled" Value="False"><Setter TargetName="Frame" Property="Opacity" Value="0.35"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style x:Key="MusicIconButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
   <Setter Property="Width" Value="40"/><Setter Property="Height" Value="36"/><Setter Property="Padding" Value="10"/>
  </Style>
  <Geometry x:Key="PreviousIcon">M 4,4 L 7,4 L 7,20 L 4,20 Z M 20,4 L 8,12 L 20,20 Z</Geometry>
  <Geometry x:Key="NextIcon">M 17,4 L 20,4 L 20,20 L 17,20 Z M 4,4 L 16,12 L 4,20 Z</Geometry>
  <Geometry x:Key="PlayIcon">M 6,3 L 21,12 L 6,21 Z</Geometry>
  <Geometry x:Key="PauseIcon">M 5,3 L 10,3 L 10,21 L 5,21 Z M 14,3 L 19,3 L 19,21 L 14,21 Z</Geometry>
  <Style x:Key="ProxySwitch" TargetType="Button">
   <Setter Property="Width" Value="48"/><Setter Property="Height" Value="28"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Margin" Value="0,0,12,0"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
    <Border x:Name="Track" Background="#565D6A" CornerRadius="14" Padding="4">
     <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="#F3F4F8" HorizontalAlignment="Left"/>
    </Border>
    <ControlTemplate.Triggers>
     <DataTrigger Binding="{Binding IsOn}" Value="True"><Setter TargetName="Track" Property="Background" Value="#53C898"/><Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/></DataTrigger>
     <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Track" Property="Opacity" Value="0.85"/></Trigger>
     <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Track" Property="Opacity" Value="0.35"/></Trigger>
    </ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style x:Key="Card" TargetType="Border"><Setter Property="Background" Value="#24262D"/><Setter Property="CornerRadius" Value="14"/><Setter Property="Padding" Value="12"/><Setter Property="Margin" Value="0,0,0,8"/></Style>
  <Style TargetType="Expander"><Setter Property="Foreground" Value="#EDF0F7"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/></Style>
  <DataTemplate x:Key="MusicCardTemplate">
   <Border Style="{StaticResource Card}" Visibility="{Binding IsVisible, Converter={StaticResource BoolVisibility}}">
    <Expander IsExpanded="{Binding IsExpanded}">
     <Expander.Header><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding DisplayName}"/><TextBlock Text="正在播放" Foreground="#81E4BA" FontSize="11" Margin="12,2,0,0" Visibility="{Binding IsPlaying, Converter={StaticResource BoolVisibility}}"/></StackPanel></Expander.Header>
     <StackPanel Margin="0,12,0,0">
      <Grid Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="64"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
       <Border Width="56" Height="56" CornerRadius="10" Background="#343C4B" HorizontalAlignment="Left" VerticalAlignment="Top">
        <Grid><TextBlock Text="♫" FontSize="27" Foreground="#98ADCA" HorizontalAlignment="Center" VerticalAlignment="Center"/><Image Source="{Binding Cover}" Stretch="UniformToFill" Width="56" Height="56"><Image.Clip><RectangleGeometry Rect="0,0,56,56" RadiusX="10" RadiusY="10"/></Image.Clip></Image></Grid>
       </Border>
       <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="{Binding Title}" ToolTip="{Binding Title}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Artist}" Foreground="#9CA8BA" FontSize="12" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/></StackPanel>
      </Grid>
      <ProgressBar Minimum="0" Maximum="{Binding DurationSeconds, Mode=OneWay}" Value="{Binding PositionSeconds, Mode=OneWay}" Height="4" Foreground="#72D8EE" Background="#394252" BorderThickness="0" IsEnabled="{Binding HasTimeline}" AutomationProperties.Name="播放进度"/>
      <Grid Margin="0,5,0,12"><TextBlock Text="{Binding PositionLabel}" FontSize="10" Foreground="#9CA8BA"/><TextBlock Text="{Binding DurationLabel}" FontSize="10" Foreground="#9CA8BA" HorizontalAlignment="Right"/></Grid>
      <WrapPanel>
       <Button Style="{StaticResource MusicIconButton}" ToolTip="上一首" AutomationProperties.Name="上一首" Command="{Binding Previous}"><Path Data="{StaticResource PreviousIcon}" Fill="#F3F4F8" Stretch="Uniform"/></Button>
       <Button Style="{StaticResource MusicIconButton}" ToolTip="{Binding PlayLabel}" AutomationProperties.Name="{Binding PlayLabel}" Command="{Binding Toggle}">
        <Path Fill="#F3F4F8" Stretch="Uniform"><Path.Style><Style TargetType="Path"><Setter Property="Data" Value="{StaticResource PlayIcon}"/><Style.Triggers><DataTrigger Binding="{Binding IsPlaying}" Value="True"><Setter Property="Data" Value="{StaticResource PauseIcon}"/></DataTrigger></Style.Triggers></Style></Path.Style></Path>
       </Button>
       <Button Style="{StaticResource MusicIconButton}" ToolTip="下一首" AutomationProperties.Name="下一首" Command="{Binding Next}"><Path Data="{StaticResource NextIcon}" Fill="#F3F4F8" Stretch="Uniform"/></Button>
       <Button Content="打开" Height="36" ToolTip="{Binding DisplayName}" Command="{Binding Open}"/>
      </WrapPanel>
      <TextBlock Text="{Binding Note}" Foreground="#E8BD78" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
     </StackPanel>
    </Expander>
   </Border>
  </DataTemplate>
  <DataTemplate x:Key="ResourceCardTemplate">
   <Border Style="{StaticResource Card}" Visibility="{Binding IsVisible, Converter={StaticResource BoolVisibility}}"><ui:SystemResourcesView/></Border>
  </DataTemplate>
  <DataTemplate x:Key="ClashCardTemplate">
   <Border Style="{StaticResource Card}" Visibility="{Binding IsVisible, Converter={StaticResource BoolVisibility}}">
    <Expander Header="Clash Verge" IsExpanded="{Binding IsExpanded}">
     <StackPanel Margin="0,12,0,0"><TextBlock Text="{Binding Status}" Foreground="#B6C6DE" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,12"/>
      <WrapPanel>
       <Button Style="{StaticResource ProxySwitch}" ToolTip="{Binding ToggleLabel}" AutomationProperties.Name="{Binding ToggleLabel}" Command="{Binding Toggle}" VerticalAlignment="Center"/>
       <Button Content="打开" Height="36" ToolTip="打开 Clash Verge" AutomationProperties.Name="打开 Clash Verge" Command="{Binding Open}"/>
      </WrapPanel>
      <TextBlock Text="{Binding Note}" Foreground="#E8BD78" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
     </StackPanel>
    </Expander>
   </Border>
  </DataTemplate>
  <DataTemplate x:Key="BalanceCardTemplate">
   <Border Style="{StaticResource Card}" Visibility="{Binding IsVisible, Converter={StaticResource BoolVisibility}}">
    <Expander Header="账户余额" IsExpanded="{Binding IsExpanded}">
     <StackPanel Margin="0,10,0,0">
      <Grid Margin="0,2,0,12">
       <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="25"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
       <StackPanel>
        <TextBlock Text="可用余额" FontSize="11" Foreground="#9CA8BA"/>
        <TextBlock Text="{Binding Balance}" FontSize="24" Foreground="#81E4BA" Margin="0,4,0,0"/>
        <TextBlock Text="{Binding Status}" FontSize="10" Foreground="#B6C6DE" TextWrapping="Wrap" Margin="0,5,0,0"/>
       </StackPanel>
       <Border Grid.Column="1" Width="1" Background="#394252" Margin="0,3"/>
       <StackPanel Grid.Column="2">
        <TextBlock Text="今日消耗" FontSize="11" Foreground="#9CA8BA"/>
        <TextBlock Text="{Binding TodayCost}" FontSize="24" Foreground="#E8BD78" Margin="0,4,0,0"/>
        <TextBlock Text="{Binding UsageStatus}" FontSize="10" Foreground="#B6C6DE" TextWrapping="Wrap" Margin="0,5,0,0"/>
       </StackPanel>
      </Grid>
      <Button Content="刷新余额与消耗" Command="{Binding Refresh}" HorizontalAlignment="Left"/>
     </StackPanel>
    </Expander>
   </Border>
  </DataTemplate>
  <DataTemplate x:Key="MonitorCardTemplate">
   <ContentControl Content="{Binding}" HorizontalContentAlignment="Stretch">
    <ContentControl.Style><Style TargetType="ContentControl">
     <Setter Property="ContentTemplate" Value="{StaticResource MusicCardTemplate}"/>
     <Style.Triggers>
      <DataTrigger Binding="{Binding Id}" Value="balance"><Setter Property="ContentTemplate" Value="{StaticResource BalanceCardTemplate}"/></DataTrigger>
      <DataTrigger Binding="{Binding Id}" Value="resources"><Setter Property="ContentTemplate" Value="{StaticResource ResourceCardTemplate}"/></DataTrigger>
      <DataTrigger Binding="{Binding Id}" Value="clash"><Setter Property="ContentTemplate" Value="{StaticResource ClashCardTemplate}"/></DataTrigger>
     </Style.Triggers>
    </Style></ContentControl.Style>
   </ContentControl>
  </DataTemplate>
 </Window.Resources>
 <Border x:Name="Capsule" Margin="10" CornerRadius="24" Background="#F018191E" BorderBrush="#FF34363F" BorderThickness="1">
  <Border.Effect><DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.35"/></Border.Effect>
  <StackPanel x:Name="RootPanel">
   <Border x:Name="Header" Padding="18,12" Background="Transparent" Cursor="Hand" MouseLeftButtonDown="BeginPointer" MouseMove="MovePointer" MouseLeftButtonUp="EndPointer" LostMouseCapture="CancelPointer">
    <StackPanel>
     <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition/><ColumnDefinition Width="28"/></Grid.ColumnDefinitions>
      <TextBlock x:Name="Heading" Grid.Column="1" Text="{Binding Heading}" HorizontalAlignment="Center" FontWeight="SemiBold" FontSize="14"/>
      <TextBlock Grid.Column="2" Text="⌄" Foreground="#949AA9" Margin="12,0,0,0"/>
     </Grid>
     <TextBlock x:Name="CompactText" ToolTip="{Binding Ai.IntegrationStatus}" HorizontalAlignment="Center" Text="{Binding Ai.Summary}" Visibility="{Binding ShowCompact, Converter={StaticResource BoolVisibility}}" TextTrimming="CharacterEllipsis" FontSize="12" Foreground="#BFC8D7" Margin="0,8,0,0"/>
     <TextBlock x:Name="ResourceCompactText" HorizontalAlignment="Center" Text="{Binding Resources.Compact}" Visibility="{Binding ShowResourceCompact, Converter={StaticResource BoolVisibility}}" FontSize="11" Foreground="#A8BCD3" Margin="0,6,0,0"/>
    </StackPanel>
   </Border>
   <ScrollViewer x:Name="DrawerScroll" Style="{StaticResource DrawerScrollStyle}" Visibility="{Binding BodyVisible, Converter={StaticResource BoolVisibility}}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="10,0,10,8">
    <StackPanel>
     <Border x:Name="AiCard" Style="{StaticResource Card}" Visibility="{Binding AiExpanded, Converter={StaticResource BoolVisibility}}">
      <StackPanel>
       <TextBlock Text="AI Agent" FontWeight="SemiBold"/>
       <StackPanel Margin="0,12,0,0">
        <TextBlock Text="等待 Codex / Claude 会话" Foreground="#9CA8BA" FontSize="12"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><DataTrigger Binding="{Binding Ai.HasRows}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
        <ItemsControl x:Name="AiGroups" ItemsSource="{Binding Ai.Groups}">
         <ItemsControl.ItemTemplate><DataTemplate><StackPanel Margin="0,0,0,8">
          <TextBlock Text="{Binding Name}" Foreground="#81E4BA" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,7"/>
          <ItemsControl ItemsSource="{Binding Rows}"><ItemsControl.ItemTemplate><DataTemplate>
           <Expander IsExpanded="{Binding IsExpanded}" Margin="0,0,0,6">
            <Expander.Header><Grid Width="290"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="88"/><ColumnDefinition Width="46"/></Grid.ColumnDefinitions>
             <Button Command="{Binding OpenTerminal}" ToolTip="返回项目终端" Background="Transparent" Padding="0" Margin="0,0,6,0" HorizontalContentAlignment="Left"><TextBlock Text="{Binding Project}" TextTrimming="CharacterEllipsis" FontSize="12"/></Button>
             <TextBlock Grid.Column="1" Text="{Binding Status}" FontSize="11" Foreground="{Binding StatusColor}" VerticalAlignment="Center"/>
             <TextBlock Grid.Column="2" Text="{Binding Elapsed}" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid></Expander.Header>
            <StackPanel Margin="20,7,0,7"><TextBlock Text="{Binding Connection}" Foreground="#E8BD78" FontSize="11"/><TextBlock Text="{Binding Details}" TextWrapping="Wrap" FontSize="11" Foreground="#9CA8BA"/><TextBlock Text="{Binding NavigationNote}" TextWrapping="Wrap" Foreground="#E8BD78" FontSize="11"/></StackPanel>
           </Expander>
          </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
         </StackPanel></DataTemplate></ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Text="{Binding Ai.Note}" Foreground="#E8BD78" FontSize="11" TextWrapping="Wrap"/>
       </StackPanel>
      </StackPanel>
     </Border>
     <StackPanel x:Name="ToolPanel" Visibility="{Binding DrawerOpen, Converter={StaticResource BoolVisibility}}">
     <ItemsControl x:Name="MonitorCards" ItemsSource="{Binding ToolModules}" ItemTemplate="{StaticResource MonitorCardTemplate}"/>
    </StackPanel>
    </StackPanel>
   </ScrollViewer>
  </StackPanel>
 </Border>
</Window>
